%tr
  %td.title
    = link_to product.name, edit_admin_product_path(product)
  %td.time
    = product.price
  %td
    %img{src: product.leading_image.image.thumb.url}
  %td
    =link_to "Usuń", admin_product_path(product), method: :delete